version: "3.9"
services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=online-shopping
    ports:
      - "5428:5432"
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local